// Assignment: =
let age = 27;
let firstName = "Douglas"
let money = 50.05;

// Arithmetic: + - * / %
age = age + 1;
age += 1;
let fullName = firstName + " MacKrell";

// Comparison: === == != > >= < <=
let canAfford = money >= 20;

// Logical: || && !
let cannotAfford = !canAfford;
let canBuyTicket = canAfford && age >= 18;